# Copy to .env and adjust. .env is loaded automatically by python-dotenv.
FLASK_APP=run.py
FLASK_DEBUG=1
SECRET_KEY=change-me-in-production

# Defaults to sqlite:///<project>/data/epadata.sqlite3 when unset.
# DATABASE_URL=sqlite:///data/epadata.sqlite3
